# Copy to .env and fill in. EXPO_PUBLIC_* vars are inlined at build time and
# are safe to expose in the client bundle (anon key is intentionally public).
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<paste-anon-public-key>
